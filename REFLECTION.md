# REFLECTION

## 1. อัตราส่วนเทสต์ต่อโค้ดจริง

เทสต์ ... ตัว / โค้ดจริง ... บรรทัด → อัตราส่วน ...
2:28
ความเห็น: ...
เพราะต้อง testให้ครอบครุม

## 2. ช่วงที่แถบเป็นสีแดงนานที่สุด

เกิดที่ขั้นตอน ... เพราะ ...
A7 เพราะต้องคิด logic ใหม่

## 3. จุดที่ใช้แต่ละเทคนิค

- **Fake It**: A1 (`self.amount = 600`) และ A6 ก้าวแรก (`plus` คืน `Quantity(500, "g")`,
  `Converter.reduce` คืนอาร์กิวเมนต์ตัวแรก) — เหมาะสม เพราะทั้งสองจุดเป็นจุดที่ยังไม่มี
  อะไรเลย A1 ยังไม่มีไฟล์ `kitchen.py` ด้วยซ้ำ สิ่งที่ได้จากการปลอมไม่ใช่โค้ด แต่คือ
  การพิสูจน์ว่าเส้นทาง test → import → assert ทำงานตั้งแต่ต้นจนจบ ส่วน A6 การบวก
  เป็นปัญหาที่ยังมองไม่เห็นทางออก (ต้องเลือกหน่วยผลลัพธ์) การมีแถบเขียวไว้ refactor
  ต่อง่ายกว่าการออกแบบ `Sum` จากศูนย์

- **Triangulate**: A2 (`test_multiplication_by_two`) — เหมาะสมในฐานะแบบฝึก แต่มองย้อน
  กลับไปแล้ว สูตร `amount * multiplier` ชัดเจนพอที่จะใช้ Obvious Implementation ได้เลย
  หลักฐานคือทันทีที่ `times` เป็นโค้ดทั่วไป เทสต์ตัวที่สองก็แทบไม่มีคุณค่าเพิ่ม
  จนโจทย์เองยังบอกให้ลบทิ้งได้ — Triangulate คุ้มค่าเมื่อยังไม่เห็นกฎทั่วไป ไม่ใช่เมื่อ
  เห็นแล้วแต่อยากทำตามพิธี

- **Obvious Implementation**: A3 (`return Quantity(self.amount * multiplier)`) และ A5
  (เพิ่มฟิลด์ `unit` เข้า `__init__`, `times`, `__eq__`, `__repr__`) — A3 เหมาะสม เพราะ
  สิ่งที่เปลี่ยนคือรูปแบบการคืนค่า ไม่ใช่ตรรกะการคำนวณ และมีเทสต์เดิมคุ้มหลังอยู่แล้ว
  ส่วน A5 เป็นก้าวที่ใหญ่เกินไป แตะสี่เมท็อดพร้อมกันในรอบเดียว ถ้าได้แถบแดงที่ไม่คาดคิด
  จะแยกไม่ออกว่าพังเพราะเมท็อดไหน ควรถอยไปแก้ทีละเมท็อดโดยให้ `unit` มีค่า default
  ชั่วคราว

- **จุดที่พลาด**: ตอนเริ่ม A6 เกือบเขียน `Sum.reduce` เป็นเวอร์ชัน polymorphic
  (`self.left.reduce(unit).amount + ...`) ทันที ทั้งที่ยังไม่มีเทสต์ตัวใดบังคับให้
  `Quantity.reduce` มีอยู่ — เป็น Obvious Implementation ที่ปนการออกแบบล่วงหน้าเข้ามา
  ตรงกับที่โจทย์เตือนว่าผู้เริ่มต้นมักใช้เทคนิคนี้มากเกินไป

## 4. รายการที่ยังค้างอยู่

**จะลงมือทำจริง**

- `200 g + 1 oz` แปลงผลลัพธ์เป็นกรัม — ต้องทำ เพราะเป็นเหตุผลทั้งหมดที่ `plus` คืน `Sum`
  แทนที่จะคำนวณทันที ถ้าไม่ทำรายการนี้ `Sum` จะกลายเป็น over-engineering ที่ไม่มีเทสต์
  ตัวใดรองรับ
- `(200 g + 1 oz) × 2` — ต้องทำต่อจากข้างบน เพราะมันจะบังคับให้เกิด `Sum` ซ้อน `Sum`
  ซึ่งเป็นแรงกดดันเดียวที่ทำให้ `Sum.reduce` ต้องเรียก `reduce` บนลูกของมันแทนการอ่าน
  `.amount` ตรง ๆ

**ควรลบทิ้งเพราะเป็นการคาดเดาล่วงหน้า**

- `__hash__` — ยังไม่มีผู้เรียกใช้รายใดต้องการใส่ `Quantity` ลง `set` หรือใช้เป็นคีย์ `dict`
  การจดไว้ในรายการไม่เสียหาย แต่การลงมือทำตอนนี้คือการเขียนโค้ดให้ผู้ใช้ในจินตนาการ
- `__eq__` กับอ็อบเจ็กต์ที่ไม่ใช่ `Quantity` — ก้ำกึ่ง ปัจจุบันมันจะพังด้วย `AttributeError`
  ซึ่งเป็นบั๊กจริง แต่ยังไม่มีโค้ดส่วนใดในระบบที่เทียบ `Quantity` กับชนิดอื่น รอให้เทสต์
  ตัวจริงมาเรียกร้องก่อนดีกว่า

## 5. Quantity.reduce

ถ้าออกแบบทั้งระบบบนกระดาษก่อน **จะไม่เขียนเมท็อดนี้ออกมา**

บนกระดาษ "การลดรูป" เป็นสิ่งที่สมเหตุสมผลกับ _นิพจน์ที่ยังไม่ถูกคำนวณ_ เท่านั้น ส่วน
`Quantity` คือค่าที่คำนวณเสร็จแล้ว การให้มันมี `reduce` ที่ `return self` ดูเหมือนโค้ดที่
ไม่ทำอะไรเลย ไม่มีเหตุผลใดบนกระดาษที่จะพาไปถึงมัน ถ้าออกแบบล่วงหน้า น่าจะจบที่การ
ให้ `Converter.reduce` เช็ค `isinstance(source, Sum)` แล้วแยกทางเอง ซึ่งเป็นการออกแบบ
ที่แย่กว่า เพราะทุกครั้งที่เพิ่มชนิดนิพจน์ใหม่ต้องกลับมาแก้ `Converter`

สิ่งที่พาไปหา `Quantity.reduce` คือแรงกดดันจากเทสต์ ไม่ใช่การออกแบบ เมื่อ `Sum` ซ้อน
`Sum` เกิดขึ้น `Sum.reduce` ต้องจัดการกับลูกที่อาจเป็น `Quantity` หรือ `Sum` ก็ได้
ทางเลือกมีสองทางคือเขียนเงื่อนไขเช็คชนิด หรือทำให้ทั้งสองคลาสมีหน้าตาเหมือนกัน
`Quantity.reduce` ที่คืน `self` จึงเกิดจากความต้องการกำจัดเงื่อนไข ไม่ใช่จากการมองเห็น
โครงสร้างล่วงหน้า

บทเรียนคือ TDD ไม่ได้บอกว่าต้องออกแบบอย่างไร แต่เทสต์ที่ล้มเหลวและความซ้ำซ้อนที่โผล่
ขึ้นมาเป็นตัวชี้ว่าการออกแบบเดิมมีปัญหาตรงไหน แล้วเราแก้ให้น้อยที่สุดเท่าที่จะแก้ปัญหาได้
ซึ่งเป็นรูปแบบเดียวกับที่เกิดขึ้นใน A3 (เทสต์เปิดโปงบั๊ก aliasing → กลายเป็น value object)
และ A4 (ความติดขัดในการเขียนเทสต์ → กลายเป็น `__eq__`)
